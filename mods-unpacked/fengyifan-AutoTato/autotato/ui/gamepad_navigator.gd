extends Node

# ============================================================================
# AutoTato — GamepadNavigator
# ----------------------------------------------------------------------------
# 手柄导航用辅助节点. Tab 切换和 ESC 处理已移至 ConfigPanel._input,
# 本节点作为扩展锚点保留, _input 留给未来自定义手柄逻辑.
# ============================================================================


func _ready() -> void:
	# Node 类型默认不接收 _input, 必须显式启用
	set_process_input(true)
